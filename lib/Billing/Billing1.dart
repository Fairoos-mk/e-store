import 'package:flutter/material.dart';
class Billing1 extends StatelessWidget {
  const Billing1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:
      Text("Billing",
        style: TextStyle(fontSize:10, color: Color(0xff1b5e20)),
      ),
    );
  }
}
